from setuptools import setup

setup(
    name="util",
    version="1.0.0",
    description="Common used parameters through optimizer and simulator",
    url="#",
    author="ksenija",
    author_email="author@email.com",
    packages=["util"],
    zip_safe=False,
)
